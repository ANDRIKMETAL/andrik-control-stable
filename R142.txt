R142 — THREE VISUAL BUG FIXES ONLY

1. On the main listener map only, in landscape, the mini-player is positioned lower and clamped inside the existing green map frame.
2. The main Admin panel gets the same magnetic landscape frame as the map; excessive empty scroll reserve is removed in portrait and landscape.
3. The mini-player button is smaller and visibly pulses everywhere, including Android reduced-motion mode.

Unchanged: mini-player JavaScript/state, analytics, Worker APIs, daily Cron, Guard, updater, swipes, map rendering, headers/eye animation and all other sections.
