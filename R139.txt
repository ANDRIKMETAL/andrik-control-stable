R139 — CONTROL RECOVERY

Исправлено:
• удалён динамический universal loader R138;
• один мини-плеер подключается прямым script/link в верхней Control-странице;
• iframe Служебного/Мониторинга/Релизов/Обсуждения не создают дубли;
• тумблер Служебного напрямую показывает и скрывает dock родителя;
• Control внутри Player Shell сразу открывается в site mode;
• большая обложка больше не появляется под админ-панелью;
• Player Shell физически вынесен за экран, но музыка может продолжать работать;
• /api/control/system не падает целиком при одной несовместимой таблице
  или необязательной службе;
• состояние системы возвращает частичные данные и показывает остальные службы;
• аналитика и анимация глаз R137/R138 сохранены.
