R729 SPECIAL UPLOAD FIX

- Спецвставки 30 и 60 минут больше не используют multipart 5 MiB + короткий финальный part.
- Новый same-origin PUT /api/control/radio-special-r729/upload?slot=30|60.
- Worker потоково и атомарно пишет MP4 в фиксированный R2 key.
- XHR показывает реальный прогресс и автоматически повторяет весь короткий файл до 4 раз при сетевом обрыве.
- Проверяется точный размер R2 object.
- Лимит спецвставки 96 MiB.
- R728: single agent, full-fit error removed, ADMIN_KEY at bottom preserved.
- Сам OVH radio engine не меняется; после деплоя R729 терминальная установка не нужна.
