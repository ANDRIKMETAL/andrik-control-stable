R721 — PERSISTENT LIVE / NO CROP / RED TITLE / SEAMLESS EQ

1. ONE persistent YouTube RTMPS publisher stays open across MP3 -> clip -> MP3.
2. Local visual/clip feeders output Annex-B H264; the persistent master uses stream-copy.
3. setts bitstream filter assigns exact monotonic 1/25 s PTS/DTS to every H264 packet.
   B-frames are disabled, so DTS=PTS is valid and feeder restarts cannot reset timestamps.
4. MORNING / DAY / EVENING / NIGHT switch inside the local feeder every 30 s check.
   Old R703/R659 systemd timers are disabled: no scheduled systemctl restart.
5. Control visual-now, visual-auto and full-fit call localhost runtime endpoints and do NOT restart radio.
6. TRUE FIT / NO CROP: scale decrease + safe pad; cropdetect and cover/increase are absent.
7. HEAVY STREAM title restored: translucent black strip, red separator, white letters, red inner outline, black outer outline/shadow.
8. Four R720 exact-periodic EQ loops are preserved: 100 frames / 4.000 s / 25 fps.
   Feeder uses continuous setpts so the 4 s -> 0 s loop has no timestamp kick.
9. Clip stall watchdog remains: a broken clip is killed locally and MP3 visual resumes on the SAME RTMPS session.
10. YouTube LIVE self-heal through Agent R721 remains active (~60 s check, ~20 s pending retry).
11. The installer performs one required service restart to move from R720 to R721. Normal runtime transitions do not restart.
