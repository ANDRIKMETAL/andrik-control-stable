ANDRIK R516 — TIKTOK DAILY GRAPH VERIFIED

- TikTok daily graph logic fixed and verified from the R515 base.
- Reads the newest 1000 TikTok snapshots (old code read the oldest 240 and would eventually freeze).
- TikTok snapshot retention raised from 240 (~10 hourly days) to 1000, enough for a full 28-day graph with headroom.
- First connected day can bootstrap from its first -> last same-day snapshot, so TikTok does not need two whole prior calendar days before appearing.
- Dedicated TikTok analytics may include today's partial daily point; the main 28-day Social Center still uses completed days only.
- One TikTok daily point now renders visibly instead of showing “need two points”. The second point automatically turns it into a line.
- Added TikTok trend diagnostics to the social-overview payload (snapshot/day counts and range) for future verification.
- Fixed stale Social Center display version R507 by adding authoritative R516 version sync.
- Worker Control release bumped to R516.
- R515 TRIKA smart-link/release changes preserved.
