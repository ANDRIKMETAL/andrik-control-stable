R803C — SINGLE WEB AGENT / DIAGNOSTICS FIX

Confirmed live topology before this build:
- andrik-radio-web.service: active, canonical R721 service, ExecStart=/usr/local/sbin/andrik-radio-web daemon
- andrik-radio-web-agent.service: active historical R650 duplicate, direct node .../andrik-radio-web-agent-r650.mjs daemon
- andrik-radio-web-control.service: masked, not used
- andrik-radio.service: active and MUST NOT be restarted by this patch

R803C changes ONLY the web/control diagnostic agents:
1. Upgrades the canonical /usr/local/sbin/andrik-radio-web binary to R803 diagnostics.
2. Restarts only andrik-radio-web.service.
3. Stops and disables the historical R650 duplicate.
4. Adds a reversible systemd Condition drop-in so R650 cannot silently resurrect.
5. Keeps pairing/token config in /etc unchanged.
6. Does not restart/stop/start andrik-radio.service, FFmpeg, producer, publisher or RTMPS.
7. Verifies local diagnosticsR803 before declaring success.
8. Proves radio MainPID before/after.
9. Public endpoint HTTP check is informational because Cloudflare may return 403 to VPS-origin curl.

Durable log:
/var/cache/andrik-radio-r622/diagnostics/r803-agent-events.ndjson

Public diagnostics:
https://andrikmetal.com/api/public/radio-diagnostics-r803
