R334 verification
Base: user-uploaded R333.

Found:
R333's */2 and */5 routing existed in the Pages project's scheduled() handler.
The user added those Cron Triggers to the separate Worker "andrik-push-cron",
so that Pages scheduled() handler cannot see controller.cron from those triggers.

Fixed:
- Added /api/automation/cron-gateway (GET/POST, CRON_SECRET protected).
- Gateway works with one CHECK_URL from the external andrik-push-cron Worker.
- 2m / 5m / 15m tasks are selected by UTC minute.
- Atomic fixed-slot D1 claims prevent duplicate work when multiple triggers overlap.
- Existing R333 WebSub, fast comments/likes, subscriber reliability and 50% quota alert retained.
- Existing central 15m automation retained.
- No phone-side polling.

Safety:
admin-sections.html / comments-admin.html / service-admin.html /
observability-admin.html / player.html / index.html / singles.html /
lyrics-admin.html unchanged byte-for-byte.
