ANDRIK Control R406 — LANDSCAPE NO FULLSCREEN TOAST

Base: confirmed stable R405 — ATOMIC AUTO SUMMARY DATA AND CLOCK.

Changed only the landscape orientation entry/exit behavior on the analytics map:
1. Removed document.documentElement.requestFullscreen() from the map orientation button.
2. Removed document.exitFullscreen() from the return-to-portrait action.
3. Kept Screen Orientation API lock('landscape') / lock('portrait'), so the installed Live Web AI PWA can rotate without triggering Chrome's browser-owned fullscreen instruction toast.
4. Daily summary/data/clock R405 logic is unchanged.
5. Current displayed Control build metadata updated to R406.

Reason: the brown message shown by Android Chrome is not an element of the site and cannot be hidden with CSS. It is generated by the browser whenever the Fullscreen API is requested. The fix removes that API call instead of masking the screen.
