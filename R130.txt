R130 — MAP BUTTON + PERSISTENT CONTROL PLAYER

• На главной карте в горизонтальном режиме закрытая кнопка находится
  в отмеченной нижней левой зоне внутри светящейся рамки.
• В остальных горизонтальных разделах кнопка остаётся внутри верхнего
  левого угла рамки.
• embed=1 больше не отключает кнопку, если страница открылась верхним уровнем.
• Настоящий вложенный iframe Control по-прежнему не создаёт дубль.
• Кнопка принудительно закрыта после загрузки, pageshow и смены ориентации.
• В Control Player Shell встроенный mini-player вынесен за экран,
  поэтому большая обложка больше не перекрывает админ-панель.
• YouTube iframe не удаляется и не получает display:none — музыка остаётся живой.
• Явная команда «Большой плеер» по-прежнему открывает полный интерфейс.
• Шапки R129, установщик, Guard и LAST KNOWN GOOD не изменены.
