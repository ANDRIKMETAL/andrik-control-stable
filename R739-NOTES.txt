R739 — timeline compensation, R738 preserved

- MP3 transport unchanged: VIDEO queue 1024 / AUDIO queue 8 / ONE RTMPS.
- R738 clip A/V PTS lock and station-insert audio handoff preserved.
- Adds runtime compensation for the stale H264 video tail seen at song boundaries.
- Default compensation: 8.0 seconds, based on the observed roughly-eight-second late transition.
- NEXT/PREVIOUS window, safe alpha fade, and CURRENT preload are all shifted by the same compensation.
- Fine tuning does NOT require a service restart:
  curl -fsS -X POST 'http://127.0.0.1:8080/control/timeline-offset?seconds=8' | python3 -m json.tool
  Change 8 to 6, 7, 9, etc. The persistent publisher and MP3 audio are not restarted.
