R803B DIAGNOSTICS WEB-CONTROL FIX

Fixes R803 installer discovery on the live OVH VPS:
- recognizes actual andrik-radio-web-control.service used by production;
- handles /usr/local/sbin/andrik-radio-web ExecStart target without .mjs extension;
- installs/restarts ONLY the web-control agent;
- never restarts andrik-radio.service, FFmpeg or RTMPS;
- removes stale standalone historical web-agent daemons only after the canonical service is healthy;
- verifies radio MainPID before/after;
- keeps R802/R801 radio behavior untouched.
