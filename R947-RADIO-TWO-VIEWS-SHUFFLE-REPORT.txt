ANDRIK R947 — RADIO TWO VIEW COUNTERS + SHUFFLE ICON

BASE: R946 preserved.

CHANGES
- Radio statistics now shows two separate counters:
  * Запуски YouTube = Data API viewCount / playback starts.
  * Просмотры = visible/validated current LIVE views from YouTube UI, with Studio/engaged fallback.
- Visible-view parser is scoped to the exact current videoId renderer and supports simpleText/runs/accessibility text.
- A static 0-view placeholder is rejected when playback starts are already non-zero.
- Last confirmed visible count is monotonic in D1 cache and cannot regress to 0.
- Queue AUTO badge replaced by standard player shuffle symbol 🔀.
- R943C server queue, R936F A/V/fullscreen code and radio247 files are untouched.
