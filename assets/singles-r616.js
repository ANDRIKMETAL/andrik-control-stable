/* ANDRIK R1159F — singles cleaned + explicit cover routing + compact Control */
(()=>{'use strict';
const root=document.getElementById('andrikSinglesList');if(!root)return;
const lang=String(document.documentElement.lang||'ru').toLowerCase().split('-')[0];
const I={
 ru:{loading:'Загружаем синглы…',empty:'Синглы скоро появятся.',play:'▶ Слушать',pause:'❚❚ Пауза',download:'↓ Скачать MP3',share:'🔗 Поделиться ссылкой',copied:'✓ Ссылка скопирована',copyPrompt:'Скопируйте ссылку для браузера',error:'Не удалось загрузить список синглов.',covers:'КАВЕРЫ',coversSub:'Отдельная коллекция каверов ANDRIK'},
 uk:{loading:'Завантажуємо сингли…',empty:'Сингли скоро з’являться.',play:'▶ Слухати',pause:'❚❚ Пауза',download:'↓ Завантажити MP3',share:'🔗 Поділитися посиланням',copied:'✓ Посилання скопійовано',copyPrompt:'Скопіюйте посилання для браузера',error:'Не вдалося завантажити список синглів.',covers:'КАВЕРИ',coversSub:'Окрема колекція каверів ANDRIK'},
 sk:{loading:'Načítavame single…',empty:'Single sa čoskoro objavia.',play:'▶ Počúvať',pause:'❚❚ Pauza',download:'↓ Stiahnuť MP3',share:'🔗 Zdieľať odkaz',copied:'✓ Odkaz skopírovaný',copyPrompt:'Skopírujte odkaz pre prehliadač',error:'Zoznam singlov sa nepodarilo načítať.',covers:'COVERY',coversSub:'Samostatná kolekcia coverov ANDRIK'},
 en:{loading:'Loading singles…',empty:'Singles are coming soon.',play:'▶ Listen',pause:'❚❚ Pause',download:'↓ Download MP3',share:'🔗 Share link',copied:'✓ Link copied',copyPrompt:'Copy this link for your browser',error:'Could not load the singles list.',covers:'COVERS',coversSub:'A separate ANDRIK covers collection'}
};
const t=I[lang]||I.ru,limit=Math.max(0,Number(root.dataset.limit||0));let fingerprint='',loading=false;
const SILENT=['Dance of Deth', 'Mind Is A Trap', 'Выбора нет', 'Жизнь идёт сама', 'Monument to the Great Void', 'I Run Away', 'Вспышка Узнавания', 'Что есть Истина', 'No choice', 'Стирай', 'Верни меня', 'Дверь освобождения', 'Ты проснулся живой', 'Сила знает путь', 'You Are Already That', 'Всё есть Брахман', 'Ты уже то', 'You Are The Light', 'Вне времени', 'Заветная звезда'];
const pretty=s=>{try{return decodeURIComponent(String(s||''))}catch(_){return String(s||'')}};const cleanTitle=s=>pretty(s).replace(/(?:\.(?:mp3|wav))+$/ig,'').trim();
const norm=s=>cleanTitle(s).toLowerCase().replace(/ё/g,'е').replace(/[«»“”„'’`]/g,'').replace(/\b(?:ai\s*)?cover\b/gi,'').replace(/кавер/giu,'').replace(/[^\p{L}\p{N}]+/gu,' ').trim().replace(/^andrik\s+/,'');
const SILENT_SET=new Set(SILENT.map(norm));
const HIDE_ALBUM_SINGLES_R1159D=['Свобода','Тишина','Ты уже достоин'];
const HIDE_ALBUM_SINGLE_SET_R1159D=new Set(HIDE_ALBUM_SINGLES_R1159D.map(norm));
const SPECIAL_COVERS_R1159F=new Set(['а я скажу нет'].map(norm));
const isCover=x=>{
  const raw=cleanTitle(x?.title||x?.name||(x?.key||'').split('/').pop());
  const key=String(x?.key||'');
  return x?.releaseType==='cover' ||
    /^covers\//i.test(key) ||
    SPECIAL_COVERS_R1159F.has(norm(raw)) ||
    /(?:^|[\s(\[—–-])(?:ai\s*)?cover(?:$|[\s)\]—–-])|кавер/iu.test(raw);
};
const esc=s=>String(s||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const fallback=s=>String(s||'').replace(/\b\w/g,c=>c.toUpperCase());
const copyLink=async(url,btn)=>{try{await navigator.clipboard.writeText(url);const old=btn.textContent;btn.textContent=t.copied;setTimeout(()=>btn.textContent=old,1600)}catch(_){window.prompt(t.copyPrompt,url)}};
function cards(a,offset=0){return a.map((x,i)=>{const title=cleanTitle(x.title).trim()||fallback(cleanTitle(x.name)),idx=i+offset;return `<article class="andrik-track" data-key="${esc(x.key||'')}"><div class="andrik-track-title">${esc(title)}</div><div class="andrik-track-actions"><button type="button" data-play="${idx}">${t.play}</button><a href="/api/music/download?key=${encodeURIComponent(x.key||'')}" download>${t.download}</a></div><button class="andrik-track-share" type="button" data-share="${esc(x.url)}">${t.share}</button><audio class="andrik-audio" data-audio="${idx}" controls preload="none" hidden src="${esc(x.url)}"></audio></article>`}).join('')}
function render(tracks){let a=Array.isArray(tracks)?[...tracks]:[];const stamp=x=>Date.parse(x?.publishedAt||x?.uploaded||0)||0;a.sort((x,y)=>stamp(y)-stamp(x)||String(y?.key||'').localeCompare(String(x?.key||''),'ru',{numeric:true,sensitivity:'base'}));const seen=new Set();a=a.filter(x=>{const n=norm(x?.title||x?.name||(x?.key||'').split('/').pop());if(!n)return false;if(!isCover(x)&&(SILENT_SET.has(n)||HIDE_ALBUM_SINGLE_SET_R1159D.has(n)))return false;if(seen.has(n))return false;seen.add(n);return true});const singles=a.filter(x=>!isCover(x)),covers=a.filter(isCover);const shown=limit?singles.slice(0,limit):singles;root.innerHTML=shown.length?cards(shown):`<div class="andrik-singles-empty">${t.empty}</div>`;document.getElementById('andrikCoversR1159')?.remove();if(!limit&&covers.length){const section=document.createElement('section');section.className='andrik-covers-r1159';section.id='andrikCoversR1159';section.innerHTML=`<div class="wrap"><div class="andrik-singles-card andrik-covers-card-r1159"><div class="andrik-singles-head"><span class="andrik-singles-kicker">ANDRIK · MUSIC</span><h2>${t.covers}</h2><p>${t.coversSub}</p></div><div class="andrik-covers-list-r1159">${cards(covers,1000)}</div></div></div>`;root.closest('.andrik-singles-r316')?.insertAdjacentElement('afterend',section)}}
async function load({force=false}={}){if(loading)return;loading=true;try{const r=await fetch(`/api/music/singles?r1159=${Date.now()}`,{cache:'no-store',headers:{'cache-control':'no-cache'}});const d=await r.json().catch(()=>({}));if(!r.ok||!d.ok)throw new Error(d.error||`HTTP ${r.status}`);const tracks=Array.isArray(d.tracks)?d.tracks:[],next=tracks.map(x=>`${x.key}|${x.publishedAt||x.uploaded||''}|${x.title||''}`).join('\n');if(force||next!==fingerprint){fingerprint=next;render(tracks)}}catch(_){if(!fingerprint)root.innerHTML=`<div class="andrik-singles-empty">${t.error}</div>`}finally{loading=false}}
function clickHandler(e){const share=e.target.closest('[data-share]');if(share){copyLink(share.dataset.share,share);return}const b=e.target.closest('[data-play]');if(!b)return;const a=document.querySelector(`[data-audio="${CSS.escape(b.dataset.play)}"]`);if(!a)return;a.hidden=false;if(a.paused){document.querySelectorAll('.andrik-audio').forEach(o=>{if(o!==a)o.pause()});document.querySelectorAll('[data-play]').forEach(x=>x.textContent=t.play);a.play().catch(()=>{});b.textContent=t.pause}else{a.pause();b.textContent=t.play}}
document.addEventListener('click',clickHandler);root.innerHTML=`<div class="andrik-singles-empty">${t.loading}</div>`;load({force:true});const interval=setInterval(()=>{if(!document.hidden)load()},limit?20000:60000);document.addEventListener('visibilitychange',()=>{if(!document.hidden)load({force:true})});window.addEventListener('storage',e=>{if(e.key==='andrik-single-r616')load({force:true})});try{const bc=new BroadcastChannel('andrik-music-r616');bc.addEventListener('message',()=>load({force:true}));window.addEventListener('pagehide',()=>bc.close(),{once:true})}catch(_){}window.addEventListener('pagehide',()=>clearInterval(interval),{once:true});
})();
