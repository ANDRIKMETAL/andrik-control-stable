(() => {
  'use strict';
  const KEY_SESSION='andrik-comments-admin-key',KEY_LOCAL='andrik-comments-admin-key-persistent';
  const byId=id=>document.getElementById(id),keyInput=byId('siteUpdateAdminKey'),archiveInput=byId('siteUpdateArchive'),previewButton=byId('siteUpdatePreview'),publishButton=byId('siteUpdatePublish'),confirmInput=byId('siteUpdateConfirm');
  let previewData=null,lastRelease='',lastPublish=null,operation=false;
  try{keyInput.value=localStorage.getItem(KEY_LOCAL)||sessionStorage.getItem(KEY_SESSION)||''}catch(_){}
  const getKey=()=>keyInput.value.trim(),sleep=ms=>new Promise(r=>setTimeout(r,ms));
  const setText=(id,text)=>{const el=byId(id);if(el)el.textContent=text};
  const escapeHtml=value=>String(value??'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#39;');
  const formatBytes=value=>{const n=Number(value||0);if(n<1024)return `${n} Б`;if(n<1024**2)return `${(n/1024).toFixed(1)} КБ`;return `${(n/1024**2).toFixed(2)} МБ`};
  const formatDate=value=>{try{return new Intl.DateTimeFormat('ru-RU',{dateStyle:'short',timeStyle:'short'}).format(new Date(value))}catch(_){return value||'—'}};
  function saveKey(){const key=getKey();try{sessionStorage.setItem(KEY_SESSION,key);if(localStorage.getItem(KEY_LOCAL))localStorage.setItem(KEY_LOCAL,key)}catch(_){}}
  async function api(path,options={}){const headers={accept:'application/json',authorization:`Bearer ${getKey()}`,...(options.headers||{})};const response=await fetch(path,{...options,headers,cache:'no-store'});const data=await response.json().catch(()=>({}));if(!response.ok){const error=new Error(data.message||data.error||`HTTP ${response.status}`);error.data=data;error.status=response.status;throw error}return data}
  function setState(ok,text){const el=byId('siteUpdateState');el.className=`service-access-state ${ok?'is-ready':'is-error'}`;el.textContent=text}
  function setResultState(kind,text){const el=byId('siteUpdateResultState');el.className=`service-access-state ${kind==='done'?'is-ready':kind==='warn'?'is-warn':kind==='error'?'is-error':''}`;el.textContent=text}
  function stage(name,status){const el=byId('siteUpdateStages')?.querySelector(`[data-stage="${name}"]`);if(!el)return;el.className=status?`is-${status}`:''}
  function resetStages(){byId('siteUpdateStages').hidden=false;['check','backup','commit','release','deploy'].forEach(name=>stage(name,''))}
  function setBusy(on){operation=on;previewButton.disabled=on||!archiveInput.files?.[0]||!getKey();publishButton.disabled=on||!confirmInput.checked||!previewData?.hasChanges;byId('siteUpdateBackupNow').disabled=on}
  function selectedFile(){return archiveInput.files?.[0]||null}
  async function loadStatus(){if(!getKey()){setState(false,'Нужен ADMIN_KEY');setText('siteUpdateConnectionMessage','Введите ключ владельца.');return}saveKey();setState(false,'Проверяем…');try{const data=await api('/api/control/site-update/status');byId('siteUpdateRepoLink').href=data.repoUrl;setText('siteUpdateRepoText',`${data.owner}/${data.repo} · ${data.branch}`);setText('siteUpdateReleaseRepoText',`Release: ${data.releaseRepository}`);if(data.connected){setState(true,`Подключено · ${data.headShort}`);setText('siteUpdateConnectionMessage',`GitHub готов. ${data.headMessage||''}`);byId('siteUpdateSetupCard').hidden=true;await Promise.allSettled([loadHistory(),loadLog()])}else{setState(false,'Не настроено');setText('siteUpdateConnectionMessage',data.message||'Нужен GitHub token.');byId('siteUpdateSetupCard').hidden=false}}catch(error){setState(false,'Ошибка');setText('siteUpdateConnectionMessage',error.message);byId('siteUpdateSetupCard').hidden=false}}
  archiveInput.addEventListener('change',()=>{const file=selectedFile();previewData=null;lastPublish=null;byId('siteUpdatePreviewCard').hidden=true;byId('siteUpdateResultCard').hidden=true;confirmInput.checked=false;resetStages();if(!file){setText('siteUpdateFileName','ZIP до 25 МБ');setText('siteUpdateFileState','Файл не выбран');previewButton.disabled=true;return}setText('siteUpdateFileName',`${file.name} · ${formatBytes(file.size)}`);setText('siteUpdateFileState','ZIP выбран');previewButton.disabled=!getKey();const match=file.name.match(/R\d+/i);if(match){lastRelease=match[0].toUpperCase();byId('siteUpdateRelease').value=lastRelease;byId('siteUpdateMessage').value=`ANDRIK Control — update website ${lastRelease}`}setText('siteUpdateUploadMessage','Нажми «Проверить ZIP».')});
  function pathsHtml(data){return [['Добавлены',data.paths?.added||[]],['Изменены',data.paths?.changed||[]],['Удалены',data.paths?.deleted||[]]].map(([title,items])=>`<section class="update-path-group"><h3>${escapeHtml(title)} · ${items.length}</h3>${items.length?items.map(path=>`<code>${escapeHtml(path)}</code>`).join(''):'<code>Нет</code>'}</section>`).join('')}
  async function preview(){const file=selectedFile();if(!file||!getKey()||operation)return;resetStages();stage('check','running');setBusy(true);setText('siteUpdateUploadMessage','Проверяем CRC, пути и сравниваем файлы…');const form=new FormData();form.append('archive',file,file.name);try{const data=await api('/api/control/site-update/preview',{method:'POST',body:form});previewData=data;stage('check','done');byId('siteUpdatePreviewCard').hidden=false;byId('siteUpdatePreviewState').textContent=data.hasChanges?'Есть изменения':'Совпадает';byId('siteUpdatePreviewState').className=`service-access-state ${data.hasChanges?'is-ready':''}`;byId('siteUpdateMetrics').innerHTML=[['Всего',data.fileCount],['＋',data.added],['～',data.changed],['−',data.deleted],['＝',data.unchanged]].map(([label,value])=>`<div class="update-metric"><strong>${Number(value||0)}</strong><span>${escapeHtml(label)}</span></div>`).join('');byId('siteUpdatePaths').innerHTML=pathsHtml(data);confirmInput.checked=false;publishButton.disabled=true;setText('siteUpdateUploadMessage',`${data.fileCount} файлов · ${formatBytes(data.totalBytes)} · текущий ${data.headShort}`);setText('siteUpdatePublishMessage',data.hasChanges?'Проверь цифры и подтверди публикацию.':'Архив совпадает с main.');byId('siteUpdatePreviewCard').scrollIntoView({behavior:'smooth',block:'start'})}catch(error){stage('check','error');previewData=null;setText('siteUpdateUploadMessage',`Ошибка: ${error.message}`)}finally{setBusy(false)}}
  confirmInput.addEventListener('change',()=>{publishButton.disabled=operation||!confirmInput.checked||!previewData?.hasChanges});
  async function createBackup(label,manual=false){const data=await api('/api/control/site-update/backup',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({label,manual})});return data}
  async function backupNow(){if(!getKey()||operation)return;setBusy(true);setText('siteUpdateConnectionMessage','Создаём резервную метку…');try{const data=await createBackup(byId('siteUpdateRelease').value||'manual',true);setText('siteUpdateConnectionMessage',data.message);await loadLog()}catch(error){setText('siteUpdateConnectionMessage',`Backup: ${error.message}`)}finally{setBusy(false)}}
  async function createRelease(file,publishData){const form=new FormData();form.append('archive',file,file.name);form.append('release',byId('siteUpdateRelease').value.trim());form.append('commitSha',publishData.commitSha||'');form.append('added',String(publishData.added||0));form.append('changed',String(publishData.changed||0));form.append('deleted',String(publishData.deleted||0));return api('/api/control/site-update/release',{method:'POST',body:form})}
  async function checkDeployment(release,quiet=false){if(!release)return null;try{const data=await api(`/api/control/site-update/deployment?release=${encodeURIComponent(release)}`);setText('siteUpdateDeployMessage',data.message);if(data.deployed){stage('deploy','done');setResultState('done','Опубликовано')}else{stage('deploy','warn');setResultState('warn','Ждёт Cloudflare')}return data}catch(error){stage('deploy','warn');if(!quiet)setText('siteUpdateDeployMessage',`Проверка Cloudflare: ${error.message}`);return null}}
  async function watchDeployment(release){stage('deploy','running');setText('siteUpdateDeployMessage',`Ищем ${release} на Control…`);for(let i=0;i<6;i++){const data=await checkDeployment(release,true);if(data?.deployed)return data;await sleep(i<2?2500:4500)}stage('deploy','warn');setResultState('warn','Ждёт Cloudflare');setText('siteUpdateDeployMessage','GitHub готов. Новая версия пока не найдена на Control. Если Pages не связан с GitHub — выполни ручной Deploy и нажми «Проверить Cloudflare».');return null}
  async function publish(){const file=selectedFile();if(!file||!previewData?.hasChanges||!confirmInput.checked||operation)return;const release=byId('siteUpdateRelease').value.trim().toUpperCase();if(!confirm(`Опубликовать ${release}?\n\n＋${previewData.added}  ～${previewData.changed}  −${previewData.deleted}`))return;resetStages();stage('check','done');byId('siteUpdateResultCard').hidden=false;setResultState('','В процессе');setBusy(true);let backupData=null,publishData=null,releaseData=null;try{if(byId('siteUpdateMakeBackup').checked){stage('backup','running');backupData=await createBackup(release);stage('backup','done')}else stage('backup','skipped');stage('commit','running');const form=new FormData();form.append('archive',file,file.name);form.append('message',byId('siteUpdateMessage').value.trim());form.append('release',release);form.append('expectedHead',previewData.headSha||'');form.append('confirm','yes');publishData=await api('/api/control/site-update/publish',{method:'POST',body:form});if(publishData.noChanges){stage('commit','done');stage('release','skipped');stage('deploy','skipped');setResultState('done','Без изменений');setText('siteUpdateResultTitle','Изменений нет');setText('siteUpdateResultText',publishData.message);return}stage('commit','done');const commitLink=byId('siteUpdateCommitLink');commitLink.href=publishData.commitUrl;commitLink.hidden=!publishData.commitUrl;if(byId('siteUpdateMakeRelease').checked){stage('release','running');try{releaseData=await createRelease(file,publishData);stage('release',releaseData.skipped?'skipped':'done');if(releaseData.releaseUrl){const link=byId('siteUpdateReleaseLink');link.href=releaseData.releaseUrl;link.hidden=false}}catch(error){stage('release','warn');releaseData={warning:error.message}}}else stage('release','skipped');lastRelease=release;lastPublish={backupData,publishData,releaseData};setText('siteUpdateResultTitle',`${release} отправлена`);setText('siteUpdateResultText',`Commit ${publishData.commitShort} · ＋${publishData.added} ～${publishData.changed} −${publishData.deleted}${backupData?` · backup ${backupData.short}`:''}${releaseData?.warning?` · Release: ${releaseData.warning}`:''}`);setResultState('','Проверяем');byId('siteUpdateResultCard').scrollIntoView({behavior:'smooth',block:'start'});previewData=null;confirmInput.checked=false;await Promise.allSettled([loadStatus(),loadHistory(),loadLog()]);watchDeployment(release)}catch(error){if(!publishData)stage('commit','error');setResultState('error','Ошибка');setText('siteUpdateResultTitle','Публикация остановлена');setText('siteUpdateResultText',error.message);setText('siteUpdatePublishMessage',`Ошибка: ${error.message}`)}finally{setBusy(false)}}
  async function loadHistory(){if(!getKey())return;try{const data=await api('/api/control/site-update/history'),box=byId('siteUpdateHistory');box.innerHTML=(data.commits||[]).map((item,index)=>`<article class="update-history-item"><div><strong>${escapeHtml(item.message||item.short)}</strong><small>${escapeHtml(item.short)} · ${escapeHtml(formatDate(item.date))}</small></div>${index===0?'<span class="service-access-state is-ready">Текущая</span>':`<button class="btn" type="button" data-rollback="${escapeHtml(item.sha)}" data-label="${escapeHtml(item.short)}">Откатить</button>`}</article>`).join('')||'<div class="admin-empty">История пуста</div>';box.querySelectorAll('[data-rollback]').forEach(button=>button.addEventListener('click',()=>rollback(button.dataset.rollback,button.dataset.label)));setText('siteUpdateHistoryMessage',`${(data.commits||[]).length} коммитов`) }catch(error){setText('siteUpdateHistoryMessage',`История: ${error.message}`)}}
  async function loadLog(){if(!getKey())return;try{const data=await api('/api/control/site-update/log'),box=byId('siteUpdateLog');const labels={'backup-created':'Backup','github-publish':'Commit','release-created':'Release','github-rollback':'Откат','github-publish-failed':'Ошибка commit','release-failed':'Ошибка Release'};box.innerHTML=(data.entries||[]).map(item=>`<article class="update-log-item is-${escapeHtml(item.level)}"><div><strong>${escapeHtml(labels[item.event]||item.event||'Событие')}</strong><small>${escapeHtml(item.message)} · ${escapeHtml(formatDate(item.createdAt))}</small></div><span class="log-dot"></span></article>`).join('')||'<div class="admin-empty">Журнал пуст</div>'}catch(error){byId('siteUpdateLog').innerHTML=`<div class="admin-empty">${escapeHtml(error.message)}</div>`}}
  async function rollback(targetSha,label){if(operation||!confirm(`Откатить сайт к ${label}?\n\nПеред откатом Control автоматически создаст backup.`))return;setBusy(true);setText('siteUpdateHistoryMessage',`Откат к ${label}…`);try{const data=await api('/api/control/site-update/rollback',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({targetSha})});setText('siteUpdateHistoryMessage',data.message);await Promise.allSettled([loadStatus(),loadHistory(),loadLog()])}catch(error){setText('siteUpdateHistoryMessage',`Ошибка: ${error.message}`)}finally{setBusy(false)}}
  byId('siteUpdateVerify').addEventListener('click',loadStatus);byId('siteUpdateRefresh').addEventListener('click',loadStatus);byId('siteUpdateBackupNow').addEventListener('click',backupNow);previewButton.addEventListener('click',preview);publishButton.addEventListener('click',publish);byId('siteUpdateHistoryRefresh').addEventListener('click',()=>Promise.allSettled([loadStatus(),loadHistory(),loadLog()]));byId('siteUpdateCheckDeploy').addEventListener('click',()=>checkDeployment(lastRelease||byId('siteUpdateRelease').value.trim().toUpperCase()));keyInput.addEventListener('input',()=>{previewButton.disabled=operation||!selectedFile()||!getKey()});keyInput.addEventListener('keydown',event=>{if(event.key==='Enter'){event.preventDefault();loadStatus()}});resetStages();if(getKey())loadStatus();else setState(false,'Нужен ADMIN_KEY');
})();
