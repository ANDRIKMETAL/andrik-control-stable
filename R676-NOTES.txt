R676 — owner push deterministic repair

- Removes the surprise reload loop at step 2.
- Adds a dedicated clean push repair page that resets only OneSignal state.
- Waits for IndexedDB deletion with no OneSignal SDK loaded.
- Verifies worker URL + Content-Type, waits for actual activation.
- Loads OneSignal only after the worker is clean and active, waits for Subscription ID.
- Passes verified ID back to Service page via same-origin sessionStorage and registers owner device.
- Restores official relative serviceWorkerPath.
- Cache reset no longer unregisters any service worker.
- Search Console R674 graph preserved.
- OVH/radio/FFmpeg/visuals untouched.
