R684 — D1 EXTRA GUARD + MAP 2 MIN + JOY OF BEING VIDEO INTERMISSION

D1 / CONTROL
1) setPushState: repeated identical values are now SQL no-ops. Timestamp/heartbeat keys still update because their values change.
2) LIVE map: 30 sec -> 2 min. Polling is fully stopped while page/tab is hidden; instant refresh when returning.
3) YouTube status on Radio page: 15 sec -> 60 sec; library count -> 5 min; both stop while hidden.
4) Radio control 5-sec status stays responsive while visible, but stops completely while hidden.
5) All R681 D1 protections preserved.
6) OVH installer refreshes and validates the QR as a real PNG, fixing the old "Invalid PNG signature" condition before restarting radio.

RADIO / JOY OF BEING
7) Official R2 video: https://music.andrikmetal.com/clips/joy-of-being-official-2026.mp4
8) Inserted once per full shuffled radio cycle, always between songs.
9) During clip: real MP4 video + its audio + QR + current yellow label + the same moving LIVE ticker.
10) After clip, scheduled day/evening/night visual resumes automatically.
11) Clip is cached locally on OVH. If R2/cache fails, radio skips the clip rather than killing the playlist.
12) One-time activation on OVH after site deploy: curl -fsSL https://andrikmetal.pages.dev/radio247/vm-lite/install-r684-joy-video.sh | sudo bash

NOTE: The YouTube broadcast itself is not ended by the clip. At clip boundaries the encoder connection is deliberately handed from the normal master to the finite video publisher and back, using the existing FIFO/timeshift cushion.
