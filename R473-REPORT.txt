R473 — YOUTUBE COMMENTS + OWNER PUSH REPAIR + 5M RESCUE SAFE FULL

Base: R472 — REMOVE OCEAN HOME PROMO SAFE FULL
Date: 2026-08-17

FIXED
1. YouTube comments no longer depend on allThreadsRelatedToChannelId.
   - The fast 2-minute path fetches comments directly by videoId.
   - Three newest uploads stay pinned in every cycle.
   - Any sampled video whose public commentCount changes is probed immediately.
   - Historical videos keep rotating inside the existing 24-video sample.
2. Likes keep the R376 safe claim/retry logic; the three newest videos are now always in the fast sample.
3. OneSignal invalid_player_ids are pruned from D1 automatically.
   - invalid owner rows are removed from push_admin_devices.
   - matching push_subscribers rows are marked inactive.
   - explicit owner rebind replaces stale ids for the same browser UA prefix.
4. Main-origin owner repair is easier.
   - Control button still opens https://andrikmetal.com/service-admin.html?owner-push=1.
   - if the owner key/session is already available, R473 automatically rebinds the current OneSignal subscription.
5. Lightweight 5-minute rescue added.
   - if the dedicated */2 engagement cron is older than 6 minutes, alternating */5 slices run the same CPU-safe engagement path.
   - release checks continue on the other */5 slices; the heavy historical reconciler is not re-enabled.
6. Service diagnostics now show direct-video comment mode and the 5-minute rescue state.

PRESERVED
- R472 home layout: no legacy OCEAN promo block.
- R471 native R2 Lyra promo video.
- R469 exact live YouTube counters.
- R416 CPU-safe split cron architecture.
- R405 atomic daily summary behavior.
- Map, planet, country UI, 98 px buttons, albums, singles and audio pipeline unchanged.

VERSION / CACHE
- Control display: R473.
- Worker release: R473.
- public service worker cache: 55.00-r473.
- cache-reset-v55-00-final-r473.html added.
