ANDRIK R502 — TIKTOK OAUTH + DISPLAY API

Base: R501.

Added:
- Official TikTok OAuth v2 start/callback/status/disconnect routes.
- Server-side encrypted TikTok access + refresh token storage in D1.
- Automatic access-token refresh using the refresh token.
- TikTok user stats: followers, following, profile likes, video count.
- TikTok public-video counters: views, likes, comments, shares.
- Social Center button “Подключить TikTok” and live TikTok metrics panel.
- Hourly TikTok server snapshots piggyback on the existing Control snapshot refresh.
- TikTok daily graph is derived only from real snapshot deltas; no fabricated historical data.
- Existing Site / YouTube / Instagram / map logic preserved.
- Version and Service Worker bumped to R502.

Cloudflare vars expected:
TIKTOK_CLIENT_KEY
TIKTOK_CLIENT_SECRET
TIKTOK_REDIRECT_URI=https://andrikmetal.com/api/oauth/tiktok/callback
TIKTOK_SCOPES=user.info.basic,user.info.stats,video.list
