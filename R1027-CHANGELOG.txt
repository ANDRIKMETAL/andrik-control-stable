ANDRIK R1027 · ACCURATE VPS MONITOR · UI ONLY
2026-09-12

1. YouTube Artist Studio
- CREATORCONTENTTYPEUNSPECIFIED теперь отображается как «Не определено».

2. Нагрузка VPS / эфира
- Убрана кнопка «УБРАТЬ СТАРЫЕ ПРОЦЕССЫ» и её UI-логика.
- CPU общий и CPU отдельных процессов теперь измеряются за ОДИН И ТОТ ЖЕ ~0.8-секундный интервал.
- Все CPU-значения приведены к одной шкале: 0–100% от всей мощности VPS (всех vCPU вместе).
- Больше нет смешивания моментального /proc/stat с усреднённым ps %CPU.
- В панели показывается длительность фактического замера.

3. Эфир
- server.mjs НЕ изменён.
- FFmpeg-фильтры, MP3, клипы, RTMPS, переходы, геометрия, QR и бегущая строка НЕ изменялись.
- Установщик VPS меняет только /usr/local/sbin/andrik-radio-load-r988.
- andrik-radio.service не перезапускается и не получает сигналов.

После установки ZIP на сайт и очистки кэша выполнить на VPS:
curl -fsSL "https://andrikmetal.com/vps-r1027-monitor-install.txt?t=$(date +%s)" | sudo bash
