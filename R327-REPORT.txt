ANDRIK R327
Base: R326 / R325 stable WAV block / R315 stable admin carousel.

Fixed:
- MP3 button is centered on its own compact second row under Text / Discussion.
- Critical inline fallback prevents the button from appearing as unstyled bare text.
- Player no longer points directly to music.andrikmetal.com for downloads.
- Added same-origin /api/music/download?key=... endpoint that streams the R2 object with Content-Disposition: attachment.
- The browser should now start a real file download instead of hanging on a top navigation/loading line.
- Track matching from R326 is retained.
- R325 OPFS large-WAV staging and global Cancel are retained.

Safety:
- admin-sections.html unchanged byte-for-byte.
- comments-admin.html unchanged byte-for-byte.
- service-admin.html unchanged byte-for-byte.
- observability-admin.html unchanged byte-for-byte.
